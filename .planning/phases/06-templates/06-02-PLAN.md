---
phase: 06-templates
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - primeaudit/test/screens/template_builder_reorder_test.dart
  - primeaudit/lib/screens/templates/template_builder_screen.dart
autonomous: true
requirements: [TMPL-02]
tags: [flutter, template-builder, reorder, drag-and-drop, reorderablelistview, wave0-test]

must_haves:
  truths:
    - "No template builder, cada seção não-vazia usa `ReorderableListView` com `onReorder` que ajusta o índice (`if (oldIndex < newIndex) newIndex -= 1;`) e persiste via `AuditTemplateService.reorderItems`"
    - "A lista de itens sem seção (`_items`) também usa `ReorderableListView` com a mesma lógica de ajuste de índice e persistência"
    - "Cada child dentro de `ReorderableListView` está envolvido em `KeyedSubtree(key: ValueKey(item.id))` — sem modificar a assinatura de `_buildItemCard`"
    - "Após arrastar e soltar, a nova ordem é persistida no banco via `reorderItems` (batch upsert) e sobrevive a close + reopen do template"
    - "Se o upsert falhar, `_showError` exibe SnackBar com `AppColors.error` e `_load()` restaura a ordem verdadeira do banco"
    - "Existe teste unitário `test/screens/template_builder_reorder_test.dart` que valida a lógica de ajuste de índice como função pura"
  artifacts:
    - path: "primeaudit/test/screens/template_builder_reorder_test.dart"
      provides: "Testes unitários TMPL-02 (3+ cenários): move-down com ajuste, move-up sem ajuste, IDs pós-reorder na ordem correta"
      contains: "List<String> applyReorder(List<String> ids, int oldIndex, int newIndex)"
    - path: "primeaudit/lib/screens/templates/template_builder_screen.dart"
      provides: "ReorderableListView substituindo flat ListView para items dentro de cada seção e para _items sem seção; helpers _persistSectionOrder e _persistUnsectionedOrder"
      contains: "ReorderableListView"
  key_links:
    - from: "ReorderableListView.onReorder (sectioned)"
      to: "_persistSectionOrder(section)"
      via: "chamada após setState com ajuste de índice"
      pattern: "_persistSectionOrder\\(section\\)"
    - from: "ReorderableListView.onReorder (unsectioned)"
      to: "_persistUnsectionedOrder()"
      via: "chamada após setState com ajuste de índice"
      pattern: "_persistUnsectionedOrder\\(\\)"
    - from: "_persistSectionOrder / _persistUnsectionedOrder"
      to: "_service.reorderItems(ids)"
      via: "batch upsert já existente (PERF-01)"
      pattern: "_service\\.reorderItems\\("
    - from: "Children de ReorderableListView"
      to: "ValueKey(item.id)"
      via: "KeyedSubtree wrapper para não alterar _buildItemCard"
      pattern: "KeyedSubtree\\(\\s*key: ValueKey\\(item\\.id\\)"
---

<objective>
Satisfazer TMPL-02: habilitar drag-and-drop de itens no `TemplateBuilderScreen` com persistência no banco. Substituir os dois trechos de flat `ListView` (itens sem seção no `build()` e itens dentro de `_buildSection()`) por `ReorderableListView` escopado por seção, chamando o já-existente `AuditTemplateService.reorderItems` (PERF-01) no callback `onReorder`.

Purpose:
- Entregar o caso de uso principal da Phase 6: admin arrasta uma pergunta e a nova ordem é salva.
- Manter `reorderItems` escopado por seção — itens de seções diferentes nunca aparecem no mesmo `ReorderableListView`, evitando o anti-pattern de cross-section reorder que corromperia `order_index`.
- Reforçar a semantic UI com drag handles visíveis (Material `ReorderableListView` default inclui um drag handle em telas pequenas e long-press em todas as plataformas).
- Reutilizar `_showError` (linha 361-366) e `_load()` (linha 38-59) como padrão de rollback em falha de persistência.

Output:
- `primeaudit/test/screens/template_builder_reorder_test.dart` criado (Wave 0) com helper `applyReorder` e 4+ cenários.
- `primeaudit/lib/screens/templates/template_builder_screen.dart` modificado (Wave 1) com:
  1. `ReorderableListView` para itens unsectioned no `build()` (substituindo `..._items.map(...)`)
  2. `ReorderableListView` para itens de seção dentro de `_buildSection()` (substituindo `...section.items.map(...)`)
  3. Helpers `_persistSectionOrder(TemplateSection section)` e `_persistUnsectionedOrder()`
- `cd primeaudit && flutter test` verde; `flutter analyze` clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-templates/06-RESEARCH.md
@.planning/phases/06-templates/06-PATTERNS.md
@.planning/phases/06-templates/06-UI-SPEC.md
@.planning/phases/06-templates/06-VALIDATION.md

<interfaces>
<!-- Tipos e código atuais que o executor precisa, extraídos diretamente do código -->

Assinatura do service (`primeaudit/lib/services/audit_template_service.dart` linhas 211-218) — JÁ EXISTE, NÃO ALTERAR:
```dart
Future<void> reorderItems(List<String> ids) async {
  if (ids.isEmpty) return;
  final payload = [
    for (int i = 0; i < ids.length; i++)
      {'id': ids[i], 'order_index': i},
  ];
  await _client.from('template_items').upsert(payload);
}
```

Helper de erro existente (`primeaudit/lib/screens/templates/template_builder_screen.dart` linhas 361-366) — usar verbatim:
```dart
void _showError(String msg) {
  if (!mounted) return;
  ScaffoldMessenger.of(context).showSnackBar(SnackBar(
    content: Text(msg), backgroundColor: AppColors.error,
    behavior: SnackBarBehavior.floating,
  ));
}
```

Assinatura de `_buildItemCard` (linha 551) — NÃO ALTERAR:
```dart
Widget _buildItemCard(TemplateItem item, {bool inSection = false}) { ... }
```

Trecho bugado A (flat list de unsectioned no `build()`, linhas 420-431 aprox):
```dart
: ListView(
    padding: const EdgeInsets.fromLTRB(16, 16, 16, 100),
    children: [
      // Itens sem seção
      if (_items.isNotEmpty) ...[
        ..._items.map((item) => _buildItemCard(item)),
        const SizedBox(height: 8),
      ],
      // Seções
      ..._sections.map((s) => _buildSection(s)),
    ],
  ),
```

Trecho bugado B (flat list dentro de `_buildSection`, linhas 535-546 aprox):
```dart
if (section.items.isEmpty)
  Padding(
    padding: const EdgeInsets.only(left: 12, bottom: 8),
    child: TextButton.icon(
      onPressed: () => _showItemForm(sectionId: section.id),
      icon: const Icon(Icons.add, size: 16),
      label: const Text('Adicionar item', style: TextStyle(fontSize: 12)),
    ),
  )
else
  ...section.items.map((item) => _buildItemCard(item, inSection: true)),
const SizedBox(height: 4),
```

Campos da State class (`primeaudit/lib/screens/templates/template_builder_screen.dart`) — `_items` é a lista mutável de itens sem seção; `_sections` é `List<TemplateSection>` onde cada `section.items` é `List<TemplateItem>` mutável.

Modelo `TemplateItem.id` (String não-nulo) — usado como `ValueKey(item.id)` requirement do `ReorderableListView`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (Wave 0): Criar teste unitário template_builder_reorder_test.dart</name>
  <files>primeaudit/test/screens/template_builder_reorder_test.dart</files>
  <read_first>
    - primeaudit/test/services/audit_template_service_reorder_test.dart (analog de estrutura — helper puro + `group` + `test` + `expect`; replicar padrão)
    - .planning/phases/06-templates/06-PATTERNS.md (seção "primeaudit/test/screens/template_builder_reorder_test.dart" — estrutura a copiar)
    - .planning/phases/06-templates/06-RESEARCH.md (seção "Pitfall 3: newIndex Not Adjusted" — explicação do pattern `if (oldIndex < newIndex) newIndex -= 1;`)
  </read_first>
  <behavior>
    - Arquivo novo criado, Flutter reconhece via glob `test/**/*_test.dart`.
    - `flutter test test/screens/template_builder_reorder_test.dart` passa GREEN — o helper `applyReorder` já implementa a lógica correta e o Task 2 aplicará a mesma lógica no screen.
    - 4 testes no `main()`:
      1. Move item down (oldIndex < newIndex) — ajuste `newIndex -= 1` é aplicado → item cai no slot correto
      2. Move item up (oldIndex > newIndex) — sem ajuste → item sobe para o slot correto
      3. Após reorder, a lista de IDs resultante é a que seria passada para `reorderItems(ids)` (ordem pós-setState)
      4. Mover para o fim da lista (newIndex == length, ajustado para length-1)
    - NÃO instanciar `TemplateBuilderScreen`, NÃO usar `testWidgets`, NÃO tocar em `Supabase.instance.client`.
    - Helper `applyReorder(List<String> ids, int oldIndex, int newIndex) -> List<String>` espelha EXATAMENTE a lógica do `onReorder` que o Task 2 vai implementar: `if (oldIndex < newIndex) newIndex -= 1; removeAt; insert`.
  </behavior>
  <action>
Passo único — criar `primeaudit/test/screens/template_builder_reorder_test.dart` com o conteúdo EXATO abaixo:

```dart
// Unit tests for TemplateBuilderScreen onReorder index logic (TMPL-02).
// Tests the reorder state mutation as a pure function — does NOT instantiate
// the screen (Supabase dependency would throw in tests).
//
// This helper mirrors the onReorder callback applied in
// primeaudit/lib/screens/templates/template_builder_screen.dart inside the
// ReorderableListView for both sectioned and unsectioned item lists.
// Kept in sync manually — any change to the screen onReorder must update the
// helper here.

import 'package:flutter_test/flutter_test.dart';

// Pure helper mirroring the onReorder callback logic in TemplateBuilderScreen.
// Matches: if (oldIndex < newIndex) newIndex -= 1; removeAt; insert.
// Returns the resulting list of IDs in their new order — this is exactly the
// list that will be passed to AuditTemplateService.reorderItems(ids).
List<String> applyReorder(List<String> ids, int oldIndex, int newIndex) {
  final list = List<String>.from(ids);
  if (oldIndex < newIndex) newIndex -= 1;
  final item = list.removeAt(oldIndex);
  list.insert(newIndex, item);
  return list;
}

void main() {
  group('TemplateBuilderScreen onReorder — index adjustment (TMPL-02)', () {
    test('move item down: index adjustment is applied (oldIndex < newIndex)', () {
      // Start: [a, b, c, d]
      // User drags 'a' (index 0) to drop at position 2 (between 'b' and 'c').
      // Flutter reports newIndex=2, but after removing 'a' all items shift
      // down; without adjustment the item would be inserted at index 2 of a
      // 3-item list, placing it after 'c' instead of before it.
      // With adjustment newIndex becomes 1 — correct slot.
      final result = applyReorder(['a', 'b', 'c', 'd'], 0, 2);
      expect(result, equals(['b', 'a', 'c', 'd']));
    });

    test('move item up: no adjustment needed (oldIndex > newIndex)', () {
      // Start: [a, b, c, d]
      // User drags 'c' (index 2) to position 0 — no adjustment.
      final result = applyReorder(['a', 'b', 'c', 'd'], 2, 0);
      expect(result, equals(['c', 'a', 'b', 'd']));
    });

    test('IDs passed to reorderItems match the new list order after reorder', () {
      // Simulates the exact flow: user reorders, onReorder updates the list,
      // the resulting ids are handed to AuditTemplateService.reorderItems.
      final original = ['item-1', 'item-2', 'item-3', 'item-4', 'item-5'];
      final afterReorder = applyReorder(original, 4, 1);
      // 'item-5' moves from index 4 to index 1 (oldIndex > newIndex, no adj).
      expect(afterReorder, equals(['item-1', 'item-5', 'item-2', 'item-3', 'item-4']));
      // reorderItems(afterReorder) would assign order_index 0..4 to these IDs
      // in this exact order — that is what the drop should persist.
    });

    test('move to end of list (newIndex == length) lands at the last slot', () {
      // Start: [a, b, c]
      // User drags 'a' (index 0) to the very end — Flutter reports
      // newIndex == list.length (3). After adjustment newIndex becomes 2,
      // then removeAt(0) + insert(2) places 'a' at the tail.
      final result = applyReorder(['a', 'b', 'c'], 0, 3);
      expect(result, equals(['b', 'c', 'a']));
    });

    test('no-op reorder (drag to same position) leaves list unchanged', () {
      // Drag index 1 to newIndex 2 — adjustment makes newIndex 1, so
      // removeAt(1) + insert(1) restores the original order.
      final result = applyReorder(['a', 'b', 'c'], 1, 2);
      expect(result, equals(['a', 'b', 'c']));
    });
  });
}
```

Regras:
- NÃO importar `package:primeaudit/...` — o teste é auto-contido.
- NÃO usar `testWidgets`, `WidgetTester`, ou mocks de Supabase.
- Idioma dos textos de `group`/`test` em inglês (segue convenção do analog).
- Caminho DEVE ser exatamente `primeaudit/test/screens/template_builder_reorder_test.dart`.
- O helper `applyReorder` usa `List<String>.from(ids)` para não mutar o argumento (função pura, sem efeito colateral).
- `if (oldIndex < newIndex) newIndex -= 1;` — exatamente essa ordem de operação (documentada em Flutter API e em RESEARCH.md Pitfall 3).
  </action>
  <verify>
    <automated>cd primeaudit && flutter test test/screens/template_builder_reorder_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `test -f primeaudit/test/screens/template_builder_reorder_test.dart` retorna sucesso.
    - `grep -c "import 'package:flutter_test/flutter_test.dart';" primeaudit/test/screens/template_builder_reorder_test.dart` retorna `1`.
    - `grep -c "List<String> applyReorder(List<String> ids, int oldIndex, int newIndex)" primeaudit/test/screens/template_builder_reorder_test.dart` retorna `1`.
    - `grep -c "if (oldIndex < newIndex) newIndex -= 1;" primeaudit/test/screens/template_builder_reorder_test.dart` retorna `1`.
    - `grep -c "list.removeAt(oldIndex)" primeaudit/test/screens/template_builder_reorder_test.dart` retorna `1`.
    - `grep -c "list.insert(newIndex, item)" primeaudit/test/screens/template_builder_reorder_test.dart` retorna `1`.
    - `grep -c "group('TemplateBuilderScreen onReorder" primeaudit/test/screens/template_builder_reorder_test.dart` retorna `1`.
    - `grep -c "test('" primeaudit/test/screens/template_builder_reorder_test.dart` retorna pelo menos `4`.
    - `grep -c "TMPL-02" primeaudit/test/screens/template_builder_reorder_test.dart` retorna pelo menos `1`.
    - `grep -c "Supabase" primeaudit/test/screens/template_builder_reorder_test.dart` retorna `0` (teste é puro).
    - `grep -c "testWidgets" primeaudit/test/screens/template_builder_reorder_test.dart` retorna `0`.
    - `cd primeaudit && flutter test test/screens/template_builder_reorder_test.dart` exit 0.
  </acceptance_criteria>
  <done>Arquivo de teste unitário criado, 5 testes (4 obrigatórios + 1 no-op extra) passam green. Helper `applyReorder` documenta a lógica de ajuste de índice que o Task 2 aplicará no `onReorder` do `ReorderableListView`.</done>
</task>

<task type="auto">
  <name>Task 2 (Wave 1): Substituir flat ListView por ReorderableListView + adicionar helpers _persistSectionOrder / _persistUnsectionedOrder</name>
  <files>primeaudit/lib/screens/templates/template_builder_screen.dart</files>
  <read_first>
    - primeaudit/lib/screens/templates/template_builder_screen.dart (arquivo completo — em particular linhas 38-59 `_load()`, 361-366 `_showError`, 382-433 `build()`, 457-549 `_buildSection`, 551+ `_buildItemCard`)
    - primeaudit/lib/services/audit_template_service.dart (linhas 211-218 — `reorderItems` já existe, apenas será consumido)
    - .planning/phases/06-templates/06-PATTERNS.md (seção "primeaudit/lib/screens/templates/template_builder_screen.dart" — código verbatim para substituições A e B, helpers `_persistSectionOrder` e `_persistUnsectionedOrder`)
    - .planning/phases/06-templates/06-RESEARCH.md (seções "Pattern 2: ReorderableListView with onReorder", "Pattern 3: Section-Scoped Reorder", "Anti-Patterns to Avoid")
    - .planning/phases/06-templates/06-UI-SPEC.md (seção "Component Inventory → New interaction: drag handle"; "Interaction States → Drag-and-drop item reorder"; "Copywriting Contract → Reorder persist error SnackBar")
    - primeaudit/test/screens/template_builder_reorder_test.dart (arquivo criado no Task 1 — comportamento do onReorder já documentado via helper `applyReorder`)
  </read_first>
  <behavior>
    - Após esta task, arrastar e soltar um item dentro de uma seção (ou na lista unsectioned) atualiza a UI imediatamente (setState otimista) e chama `reorderItems` com a lista de IDs na nova ordem.
    - Se `reorderItems` lançar, `_showError('Erro ao salvar nova ordem. A ordem foi restaurada.')` exibe SnackBar com `AppColors.error` e `_load()` recarrega do banco (restaura ordem real).
    - Cada `ReorderableListView` tem `shrinkWrap: true` e `physics: const NeverScrollableScrollPhysics()` (pois o outer `ListView` do `build()` é quem rola; `ReorderableListView` fica aninhado).
    - Testes do Task 1 continuam GREEN; `flutter test` suíte completa green; `flutter analyze` clean.
    - Nenhum cross-section reorder — um `ReorderableListView` por seção, um para `_items` unsectioned. Seções NÃO são reordenáveis neste plano (só os items dentro delas).
    - `_buildItemCard` NÃO tem sua assinatura alterada — `KeyedSubtree(key: ValueKey(item.id), child: _buildItemCard(item, ...))` é o wrapper que satisfaz o requirement de key do ReorderableListView.
  </behavior>
  <action>
Executar TRÊS substituições pontuais e UMA adição de métodos no arquivo `primeaudit/lib/screens/templates/template_builder_screen.dart`.

Passo A — Substituição do bloco de itens sem seção no `build()` (atual linhas 424-427 aprox, identificáveis via `grep -n "if (_items.isNotEmpty)" primeaudit/lib/screens/templates/template_builder_screen.dart`).

REMOVER EXATAMENTE:
```dart
                    // Itens sem seção
                    if (_items.isNotEmpty) ...[
                      ..._items.map((item) => _buildItemCard(item)),
                      const SizedBox(height: 8),
                    ],
```

INSERIR EXATAMENTE NO MESMO LOCAL:
```dart
                    // Itens sem seção — drag & drop via ReorderableListView (TMPL-02)
                    if (_items.isNotEmpty) ...[
                      ReorderableListView(
                        shrinkWrap: true,
                        physics: const NeverScrollableScrollPhysics(),
                        buildDefaultDragHandles: true,
                        onReorder: (int oldIndex, int newIndex) {
                          if (oldIndex < newIndex) newIndex -= 1;
                          setState(() {
                            final item = _items.removeAt(oldIndex);
                            _items.insert(newIndex, item);
                          });
                          _persistUnsectionedOrder();
                        },
                        children: [
                          for (final item in _items)
                            KeyedSubtree(
                              key: ValueKey(item.id),
                              child: _buildItemCard(item),
                            ),
                        ],
                      ),
                      const SizedBox(height: 8),
                    ],
```

Passo B — Substituição do bloco de itens dentro de `_buildSection()` (atual linhas 535-546 aprox, identificáveis via `grep -n "...section.items.map" primeaudit/lib/screens/templates/template_builder_screen.dart`).

REMOVER EXATAMENTE:
```dart
        if (section.items.isEmpty)
          Padding(
            padding: const EdgeInsets.only(left: 12, bottom: 8),
            child: TextButton.icon(
              onPressed: () => _showItemForm(sectionId: section.id),
              icon: const Icon(Icons.add, size: 16),
              label: const Text('Adicionar item', style: TextStyle(fontSize: 12)),
            ),
          )
        else
          ...section.items.map((item) => _buildItemCard(item, inSection: true)),
        const SizedBox(height: 4),
```

INSERIR EXATAMENTE NO MESMO LOCAL:
```dart
        if (section.items.isEmpty)
          Padding(
            padding: const EdgeInsets.only(left: 12, bottom: 8),
            child: TextButton.icon(
              onPressed: () => _showItemForm(sectionId: section.id),
              icon: const Icon(Icons.add, size: 16),
              label: const Text('Adicionar item', style: TextStyle(fontSize: 12)),
            ),
          )
        else
          // Items da seção — drag & drop via ReorderableListView (TMPL-02).
          // Uma ReorderableListView POR seção — cross-section reorder é out of scope.
          ReorderableListView(
            shrinkWrap: true,
            physics: const NeverScrollableScrollPhysics(),
            buildDefaultDragHandles: true,
            onReorder: (int oldIndex, int newIndex) {
              if (oldIndex < newIndex) newIndex -= 1;
              setState(() {
                final item = section.items.removeAt(oldIndex);
                section.items.insert(newIndex, item);
              });
              _persistSectionOrder(section);
            },
            children: [
              for (final item in section.items)
                KeyedSubtree(
                  key: ValueKey(item.id),
                  child: _buildItemCard(item, inSection: true),
                ),
            ],
          ),
        const SizedBox(height: 4),
```

Passo C — Adicionar DOIS novos métodos privados na State class `_TemplateBuilderScreenState`. Inserir imediatamente ANTES do método `_showError` (linha 361 aprox — usar `grep -n "void _showError" primeaudit/lib/screens/templates/template_builder_screen.dart` para localizar):

```dart
  // TMPL-02: persiste a nova ordem dos itens de uma seção após drag & drop.
  // Em caso de falha, exibe SnackBar de erro e recarrega do banco para
  // restaurar a ordem verdadeira (padrão do projeto — screens reload on error).
  Future<void> _persistSectionOrder(TemplateSection section) async {
    try {
      await _service.reorderItems(section.items.map((i) => i.id).toList());
    } catch (e) {
      _showError('Erro ao salvar nova ordem. A ordem foi restaurada.');
      _load();
    }
  }

  // TMPL-02: persiste a nova ordem dos itens sem seção após drag & drop.
  // Mesma semântica do _persistSectionOrder, mas para `_items`.
  Future<void> _persistUnsectionedOrder() async {
    try {
      await _service.reorderItems(_items.map((i) => i.id).toList());
    } catch (e) {
      _showError('Erro ao salvar nova ordem. A ordem foi restaurada.');
      _load();
    }
  }

```

Regras CRÍTICAS:
- `ReorderableListView` EXIGE `shrinkWrap: true` + `physics: const NeverScrollableScrollPhysics()` quando aninhado em outro scrollable (aqui, o `ListView` do `build()`). Sem isso, crasha com unbounded height.
- `buildDefaultDragHandles: true` é o DEFAULT, mas explícito para clareza — habilita o drag handle built-in do Flutter Material 3 no trailing de cada tile (consistente com `Icons.drag_handle_rounded` já usado em `_showItemForm`, UI-SPEC.md seção "New interaction").
- NÃO modificar a assinatura de `_buildItemCard` — `KeyedSubtree` resolve o requirement de key.
- `ValueKey(item.id)` — `item.id` é String não-nulo (confirmado pelo modelo `TemplateItem`).
- Mensagem EXATA do erro: `'Erro ao salvar nova ordem. A ordem foi restaurada.'` — casa com UI-SPEC.md "Copywriting Contract → Reorder persist error SnackBar".
- Capturar `catch (e)` — O valor `e` é deliberadamente não usado (só o fato de ter falhado importa; o usuário não precisa do detalhe técnico). Se o analyzer reclamar de `unused_catch_clause` por causa do `e` não utilizado, trocar para `catch (_)` — ambas são aceitáveis.
- NÃO adicionar `reorderItems` com IDs mixados entre seções — cada chamada é scoped a uma seção (items daquela seção) OU a `_items` unsectioned. RESEARCH.md Anti-Pattern "Calling reorderItems with section-mixed IDs" explícito.
- NÃO reordenar seções inteiras neste plano — só items dentro delas. Seções permanecem renderizadas via `..._sections.map((s) => _buildSection(s))` no `build()` (sem `ReorderableListView` externo).
- NÃO remover `const SizedBox(height: 4)` do final de `_buildSection` — é parte do spacing da UI (mantido no bloco substituído).
- NÃO tocar em `_showItemForm`, `_addSection`, `_buildEmpty`, `_responseTypes`, `_buildItemCard`, header da seção, PopupMenuButton da seção, nem em qualquer outro método fora dos 3 passos acima.
  </action>
  <verify>
    <automated>cd primeaudit && flutter analyze lib/screens/templates/template_builder_screen.dart &amp;&amp; flutter test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ReorderableListView(" primeaudit/lib/screens/templates/template_builder_screen.dart` retorna exatamente `2` (um para unsectioned, um para sectioned).
    - `grep -c "if (oldIndex < newIndex) newIndex -= 1;" primeaudit/lib/screens/templates/template_builder_screen.dart` retorna exatamente `2`.
    - `grep -c "KeyedSubtree(" primeaudit/lib/screens/templates/template_builder_screen.dart` retorna pelo menos `2` (um em cada ReorderableListView.children).
    - `grep -c "ValueKey(item.id)" primeaudit/lib/screens/templates/template_builder_screen.dart` retorna pelo menos `2`.
    - `grep -c "shrinkWrap: true," primeaudit/lib/screens/templates/template_builder_screen.dart` retorna pelo menos `2`.
    - `grep -c "NeverScrollableScrollPhysics()" primeaudit/lib/screens/templates/template_builder_screen.dart` retorna pelo menos `2`.
    - `grep -c "Future<void> _persistSectionOrder(TemplateSection section)" primeaudit/lib/screens/templates/template_builder_screen.dart` retorna `1`.
    - `grep -c "Future<void> _persistUnsectionedOrder()" primeaudit/lib/screens/templates/template_builder_screen.dart` retorna `1`.
    - `grep -c "_service.reorderItems(section.items.map((i) => i.id).toList())" primeaudit/lib/screens/templates/template_builder_screen.dart` retorna `1`.
    - `grep -c "_service.reorderItems(_items.map((i) => i.id).toList())" primeaudit/lib/screens/templates/template_builder_screen.dart` retorna `1`.
    - `grep -c "'Erro ao salvar nova ordem. A ordem foi restaurada.'" primeaudit/lib/screens/templates/template_builder_screen.dart` retorna `2` (um em cada helper).
    - `grep -c "\\.\\.\\._items.map((item) => _buildItemCard(item))" primeaudit/lib/screens/templates/template_builder_screen.dart` retorna `0` (linha ANTIGA da flat list unsectioned removida).
    - `grep -c "\\.\\.\\.section.items.map((item) => _buildItemCard(item, inSection: true))" primeaudit/lib/screens/templates/template_builder_screen.dart` retorna `0` (linha ANTIGA da flat list sectioned removida).
    - `grep -c "Widget _buildItemCard(TemplateItem item, {bool inSection = false})" primeaudit/lib/screens/templates/template_builder_screen.dart` retorna `1` (assinatura de `_buildItemCard` preservada, inalterada).
    - `grep -c "void _showError(String msg)" primeaudit/lib/screens/templates/template_builder_screen.dart` retorna `1` (helper existente preservado).
    - `grep -c "buildDefaultDragHandles: true" primeaudit/lib/screens/templates/template_builder_screen.dart` retorna `2`.
    - `cd primeaudit && flutter analyze lib/screens/templates/template_builder_screen.dart` imprime `No issues found!`.
    - `cd primeaudit && flutter test` exit 0 (suíte completa: audit_template_service_reorder_test.dart + template_builder_reorder_test.dart + audit_execution_ordering_test.dart + testes pré-existentes, todos green).
  </acceptance_criteria>
  <done>Drag & drop habilitado em duas listas (sectioned e unsectioned), persistência via `reorderItems` com rollback em falha, `_buildItemCard` intocado, zero cross-section reorder, analyze clean, suíte verde. TMPL-02 entregue.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Este plano NÃO introduz novos trust boundaries. A única escrita no banco é o `UPDATE template_items SET order_index = ...` executado por `AuditTemplateService.reorderItems`, que já existe desde Phase 4 (PERF-01) e é coberto pela RLS existente em `template_items` (permite UPDATE apenas para `adm`, `superuser`, `dev`).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | T (Tampering) | Auditor tenta reordenar items via drag na UI do admin | mitigate | A tela `TemplateBuilderScreen` já é gated por role (apenas `adm`/`superuser`/`dev` chegam nela — gate existente em drawer / rota). Além disso, RLS existente em `template_items` bloqueia UPDATE de `order_index` se algum bypass de UI chegar ao banco com role `auditor`. Assumption A1 em RESEARCH.md — validar RLS antes do deploy. Nenhum novo surface. |
| T-06-02-02 | T (Tampering) | Out-of-bounds `newIndex` no `onReorder` | mitigate | Lógica de ajuste `if (oldIndex < newIndex) newIndex -= 1;` + `List.removeAt`/`insert` da Dart validam bounds e lançam `RangeError` antes de chegar ao DB. Teste unitário `template_builder_reorder_test.dart` valida o caso de `newIndex == length` (move para o fim). |
| T-06-02-03 | I (Information Disclosure) | Mensagem de erro no SnackBar | accept | Mensagem é genérica (`'Erro ao salvar nova ordem. A ordem foi restaurada.'`) — não expõe detalhes internos (`e.toString()` não vaza). Aceito. |
| T-06-02-04 | D (DoS) | Upsert batch com N IDs por reorder | accept | `reorderItems` faz UPSERT único (PERF-01) — O(1) round-trip, não O(N). Escopo por seção mantém N pequeno (tipicamente 5-50). Rate limit natural pela velocidade de drag humano. |
| T-06-02-05 | — | Novos fluxos de auth / rede / storage | accept | Nenhum — reusa `_client.from('template_items').upsert(...)` existente. RESEARCH.md Security Domain confirma: "No new security surface introduced by this phase." |
</threat_model>

<verification>
- `cd primeaudit && flutter analyze lib/screens/templates/template_builder_screen.dart` → `No issues found!`.
- `cd primeaudit && flutter test test/screens/template_builder_reorder_test.dart` → exit 0 (5 testes passam).
- `cd primeaudit && flutter test` → exit 0 (suíte completa green, incluindo o test existente `audit_template_service_reorder_test.dart` e o test do Plan 06-01).
- `git diff primeaudit/lib/screens/templates/template_builder_screen.dart` mostra:
  1. Dois blocos de `ReorderableListView` substituindo os dois spreads de `.map(...)` (build e _buildSection).
  2. Dois novos métodos `_persistSectionOrder` / `_persistUnsectionedOrder` adicionados antes de `_showError`.
  3. NENHUMA alteração em `_showItemForm`, `_addSection`, `_buildEmpty`, `_buildItemCard`, header da seção, PopupMenuButton, imports.
- `git status` mostra `primeaudit/test/screens/template_builder_reorder_test.dart` como untracked (novo).
- Verificação manual (registrada em UAT): long-press + drag em item muda posição; close + reopen do template mostra ordem persistida; dropping com rede offline mostra SnackBar de erro e ordem restaurada.
</verification>

<success_criteria>
- TMPL-02: Usuário pode reordenar perguntas no template builder via drag & drop com persistência no banco. VERIFICÁVEL: grep confirma 2x `ReorderableListView` + 2x `_service.reorderItems` + helpers; teste unitário confirma lógica de ajuste de índice.
- Phase success criterion 2: "admin pode arrastar uma pergunta para cima ou para baixo e a nova ordem é salva no banco após soltar" — entregue via `onReorder` + `_persistSectionOrder` / `_persistUnsectionedOrder`.
- Phase success criterion 3: "Após reordenar e fechar o template builder, reabrir o template mostra a nova ordem persistida" — garantido pelo batch upsert de `reorderItems` já existente (PERF-01) + `getItems` com `.order('order_index')` no próximo load.
- Wave 0 gap fechado: `primeaudit/test/screens/template_builder_reorder_test.dart` existe e executa.
- Escopo preservado: sem novas migrations, sem novos packages, sem mudanças em service ou model; `_buildItemCard` signature intocada; cross-section reorder não habilitado.
- Nenhuma regressão: suíte `flutter test` continua verde.
</success_criteria>

<output>
After completion, create `.planning/phases/06-templates/06-02-SUMMARY.md` describing:
- Diff resumido de `primeaudit/lib/screens/templates/template_builder_screen.dart` (linhas adicionadas/removidas por região)
- Trecho final do `ReorderableListView` unsectioned (no `build()`) — para UAT humana confirmar visualmente
- Trecho final do `ReorderableListView` sectioned (no `_buildSection`)
- Trecho final de `_persistSectionOrder` e `_persistUnsectionedOrder`
- Resultado de `flutter analyze` e `flutter test`
- Status do Wave 0 gap de TMPL-02 (closed)
- Qualquer desvio observado (por exemplo, se `buildDefaultDragHandles: true` teve que ser trocado por custom drag handle por razão específica da plataforma, ou se o analyzer exigiu `catch (_)` em vez de `catch (e)`)
- Itens manuais pendentes para UAT: long-press + drag no emulador, close + reopen confirmando persistência, teste de falha de rede simulando rollback
</output>
