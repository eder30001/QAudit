---
phase: 06-templates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - primeaudit/test/screens/audit_execution_ordering_test.dart
  - primeaudit/lib/screens/audit_execution_screen.dart
autonomous: true
requirements: [TMPL-01]
tags: [flutter, audit-execution, sort, grouping, order-index, wave0-test]

must_haves:
  truths:
    - "Após o agrupamento por `sectionId` em `AuditExecutionScreen._load()`, cada bucket está ordenado por `orderIndex` crescente"
    - "O bucket de itens sem seção (`unsectioned`) está ordenado por `orderIndex` crescente antes de virar a seção fictícia 'Geral'"
    - "Na tela de execução, os itens dentro de cada seção (e da seção 'Geral') aparecem na ordem definida por `order_index`, independente da ordem em que o PostgREST retornou ou em que itens foram inseridos"
    - "Existe teste unitário `test/screens/audit_execution_ordering_test.dart` que prova o comportamento do sort como função pura"
    - "Nenhum outro método de `AuditExecutionScreen` foi alterado além do trecho de agrupamento dentro de `_load()`"
  artifacts:
    - path: "primeaudit/test/screens/audit_execution_ordering_test.dart"
      provides: "Testes unitários TMPL-01 (3 cenários): sort dentro da seção, sort do bucket unsectioned, correção de inserção out-of-order"
      contains: "group('AuditExecutionScreen._load()"
    - path: "primeaudit/lib/screens/audit_execution_screen.dart"
      provides: "Fix do bug de ordenação: bucket.sort + unsectioned.sort após agrupamento em _load()"
      contains: "bucket.sort((a, b) => a.orderIndex.compareTo(b.orderIndex))"
  key_links:
    - from: "primeaudit/lib/screens/audit_execution_screen.dart:_load()"
      to: "TemplateItem.orderIndex"
      via: "comparator dentro de `bucket.sort`"
      pattern: "a\\.orderIndex\\.compareTo\\(b\\.orderIndex\\)"
    - from: "primeaudit/test/screens/audit_execution_ordering_test.dart"
      to: "groupAndSort helper (mirrors _load logic)"
      via: "pure function test mirrando o código sob teste"
      pattern: "groupAndSort\\("
---

<objective>
Satisfazer TMPL-01: corrigir o bug de ordenação silencioso no `AuditExecutionScreen._load()` — itens agrupados em buckets de seção via `putIfAbsent + add` perdem a ordem do `.order('order_index')` do PostgREST. O fix é uma adição de 2 linhas (`bucket.sort` + `unsectioned.sort`) imediatamente após o agrupamento, preservando `orderIndex` ascendente dentro de cada bucket.

Purpose:
- Garantir que a tela de execução sempre exiba itens na ordem definida por `order_index`, pré-condição para que o drag-and-drop do Plan 06-02 tenha efeito visível.
- Cobrir o comportamento por teste unitário como função pura (sem instanciar o widget, sem Supabase) — segue padrão de `test/services/audit_template_service_reorder_test.dart`.

Output:
- `primeaudit/test/screens/audit_execution_ordering_test.dart` criado (Wave 0), contendo 3 cenários e um helper `groupAndSort` que espelha a lógica sob teste.
- `primeaudit/lib/screens/audit_execution_screen.dart` modificado (Wave 1): bloco de agrupamento em `_load()` adiciona `bucket.sort` dentro do loop de sections e `unsectioned.sort` após extrair o bucket de null.
- `cd primeaudit && flutter test` verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-templates/06-RESEARCH.md
@.planning/phases/06-templates/06-PATTERNS.md
@.planning/phases/06-templates/06-UI-SPEC.md
@.planning/phases/06-templates/06-VALIDATION.md

<interfaces>
<!-- Tipos e código atuais que o executor precisa, extraídos diretamente do código -->

Do modelo `TemplateItem` (`primeaudit/lib/models/audit_template.dart`):
```dart
class TemplateItem {
  final String id;
  final String templateId;
  final String? sectionId;    // null quando item não pertence a nenhuma seção
  final String question;
  final String responseType;
  final double weight;
  final bool required;
  final int orderIndex;        // campo-chave para o sort
  // ...construtor, fromMap...
}
```

Do modelo `TemplateSection` (`primeaudit/lib/models/audit_template.dart`):
```dart
class TemplateSection {
  final String id;
  final String templateId;
  final String name;
  final int orderIndex;
  List<TemplateItem> items;    // populado em tempo de execução — este é o bucket que precisa estar ordenado
  // ...
}
```

Do analog de teste (`primeaudit/test/services/audit_template_service_reorder_test.dart`, linhas 1-47) — padrão exato a replicar: helper puro + `group` + `test` + `expect`. NÃO usar `testWidgets`, NÃO instanciar screen, NÃO tocar em Supabase.

Código atual bugado em `primeaudit/lib/screens/audit_execution_screen.dart` (linhas 73-83):
```dart
// Associa items às seções
final itemsBySection = <String?, List<TemplateItem>>{};
for (final item in items) {
  itemsBySection.putIfAbsent(item.sectionId, () => []).add(item);
}
for (final s in sections) {
  s.items = itemsBySection[s.id] ?? [];
}

// Items sem seção ficam numa seção fictícia "Geral"
final unsectioned = itemsBySection[null] ?? [];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (Wave 0): Criar teste unitário audit_execution_ordering_test.dart</name>
  <files>primeaudit/test/screens/audit_execution_ordering_test.dart</files>
  <read_first>
    - primeaudit/test/services/audit_template_service_reorder_test.dart (analog exato — padrão de helper puro + `group` + `test` + `expect`; replicar estrutura)
    - .planning/phases/06-templates/06-PATTERNS.md (seção "primeaudit/test/screens/audit_execution_ordering_test.dart" — estrutura completa a copiar verbatim)
    - .planning/phases/06-templates/06-RESEARCH.md (seção "Validation Architecture" — lista dos 3 cenários obrigatórios)
    - primeaudit/lib/screens/audit_execution_screen.dart (linhas 73-83 — código bugado que será espelhado pelo helper `groupAndSort`)
  </read_first>
  <behavior>
    - Arquivo novo criado, suíte Flutter reconhece via glob `test/**/*_test.dart`.
    - `flutter test test/screens/audit_execution_ordering_test.dart` roda e passa GREEN (o helper já implementa o sort correto — o teste está validando o comportamento CORRIGIDO que o Task 2 vai aplicar ao screen).
    - 3 testes no `main()`:
      1. items de uma seção são sorted por `orderIndex` após grouping
      2. bucket unsectioned é sorted por `orderIndex`
      3. inserção out-of-order é corrigida pelo sort (ex: inserir `orderIndex=5` depois de `orderIndex=1` — resultado final ordem 1,5)
    - NÃO instanciar `AuditExecutionScreen`, NÃO usar `testWidgets`, NÃO tocar em `Supabase.instance.client`.
    - Tipo fake `_FakeItem` definido no arquivo com apenas `id`, `sectionId`, `orderIndex` (minimo suficiente para o sort — não espelhar todo `TemplateItem`).
  </behavior>
  <action>
Passo único — criar o arquivo `primeaudit/test/screens/audit_execution_ordering_test.dart` com o conteúdo EXATO abaixo. Copiar verbatim:

```dart
// Unit tests for AuditExecutionScreen._load() grouping sort correctness (TMPL-01).
// Tests the bucket sort logic as a pure function — does NOT instantiate the
// screen (Supabase.instance.client would throw in tests).
//
// This helper mirrors the grouping + sort logic applied in
// primeaudit/lib/screens/audit_execution_screen.dart inside _load(). Kept in
// sync manually — any change to the screen grouping must also update the
// helper here.

import 'package:flutter_test/flutter_test.dart';

// Minimal item representation — mirrors the fields of TemplateItem used by the
// grouping/sort logic. Keep in sync with lib/models/audit_template.dart.
class _FakeItem {
  final String id;
  final String? sectionId;
  final int orderIndex;
  const _FakeItem({
    required this.id,
    this.sectionId,
    required this.orderIndex,
  });
}

// Pure helper mirroring the grouping + sort logic in
// AuditExecutionScreen._load(). After the FIX applied in Task 2, each
// bucket (including the unsectioned bucket at key `null`) is sorted by
// orderIndex ascending.
Map<String?, List<_FakeItem>> groupAndSort(List<_FakeItem> items) {
  final bySection = <String?, List<_FakeItem>>{};
  for (final item in items) {
    bySection.putIfAbsent(item.sectionId, () => []).add(item);
  }
  for (final entry in bySection.entries) {
    entry.value.sort((a, b) => a.orderIndex.compareTo(b.orderIndex));
  }
  return bySection;
}

void main() {
  group('AuditExecutionScreen._load() — grouping sort (TMPL-01)', () {
    test('items within a section are sorted by orderIndex after grouping', () {
      final items = <_FakeItem>[
        _FakeItem(id: 'b', sectionId: 's1', orderIndex: 1),
        _FakeItem(id: 'a', sectionId: 's1', orderIndex: 0),
        _FakeItem(id: 'c', sectionId: 's1', orderIndex: 2),
      ];

      final result = groupAndSort(items);

      expect(result['s1']!.map((i) => i.id).toList(), equals(['a', 'b', 'c']));
      expect(result['s1']!.map((i) => i.orderIndex).toList(), equals([0, 1, 2]));
    });

    test('unsectioned items bucket is sorted by orderIndex', () {
      final items = <_FakeItem>[
        _FakeItem(id: 'y', orderIndex: 2),
        _FakeItem(id: 'z', orderIndex: 3),
        _FakeItem(id: 'x', orderIndex: 1),
      ];

      final result = groupAndSort(items);

      expect(result[null], isNotNull);
      expect(result[null]!.map((i) => i.id).toList(), equals(['x', 'y', 'z']));
      expect(result[null]!.map((i) => i.orderIndex).toList(), equals([1, 2, 3]));
    });

    test('out-of-order insertion is corrected by sort', () {
      // Simulates PostgREST returning items in a different order than orderIndex
      // (e.g., after a recent reorder where the cache is stale), or a new item
      // inserted with orderIndex=5 before the list was re-sorted.
      final items = <_FakeItem>[
        _FakeItem(id: 'first',  sectionId: 's1', orderIndex: 1),
        _FakeItem(id: 'last',   sectionId: 's1', orderIndex: 5),
        _FakeItem(id: 'middle', sectionId: 's1', orderIndex: 3),
      ];

      final result = groupAndSort(items);

      expect(
        result['s1']!.map((i) => i.id).toList(),
        equals(['first', 'middle', 'last']),
      );
      expect(
        result['s1']!.map((i) => i.orderIndex).toList(),
        equals([1, 3, 5]),
      );
    });

    test('multiple sections are each sorted independently', () {
      final items = <_FakeItem>[
        _FakeItem(id: 'a2', sectionId: 'A', orderIndex: 1),
        _FakeItem(id: 'b1', sectionId: 'B', orderIndex: 0),
        _FakeItem(id: 'a1', sectionId: 'A', orderIndex: 0),
        _FakeItem(id: 'b2', sectionId: 'B', orderIndex: 1),
      ];

      final result = groupAndSort(items);

      expect(result['A']!.map((i) => i.id).toList(), equals(['a1', 'a2']));
      expect(result['B']!.map((i) => i.id).toList(), equals(['b1', 'b2']));
    });
  });
}
```

Regras:
- NÃO adicionar `import 'package:primeaudit/...'` — o teste é auto-contido via `_FakeItem` para evitar acoplar à definição real de `TemplateItem`.
- NÃO usar `setUp` / `tearDown` — não há estado compartilhado.
- NÃO adicionar testes de widget (`testWidgets`) ou mocks de Supabase.
- Idioma dos textos de `group`/`test` em inglês (segue convenção do analog `audit_template_service_reorder_test.dart`).
- Caminho DEVE ser exatamente `primeaudit/test/screens/audit_execution_ordering_test.dart` — diretório `test/screens/` ainda pode não existir; o comando `Write` deve criá-lo se necessário (Windows: separador `\`; Git Bash: `/` — usar `/` no caminho).
  </action>
  <verify>
    <automated>cd primeaudit && flutter test test/screens/audit_execution_ordering_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `test -f primeaudit/test/screens/audit_execution_ordering_test.dart` retorna sucesso (arquivo existe).
    - `grep -c "import 'package:flutter_test/flutter_test.dart';" primeaudit/test/screens/audit_execution_ordering_test.dart` retorna `1`.
    - `grep -c "class _FakeItem" primeaudit/test/screens/audit_execution_ordering_test.dart` retorna `1`.
    - `grep -c "Map<String?, List<_FakeItem>> groupAndSort" primeaudit/test/screens/audit_execution_ordering_test.dart` retorna `1`.
    - `grep -c "a.orderIndex.compareTo(b.orderIndex)" primeaudit/test/screens/audit_execution_ordering_test.dart` retorna `1`.
    - `grep -c "group('AuditExecutionScreen._load()" primeaudit/test/screens/audit_execution_ordering_test.dart` retorna `1`.
    - `grep -c "test('" primeaudit/test/screens/audit_execution_ordering_test.dart` retorna pelo menos `3`.
    - `grep -c "TMPL-01" primeaudit/test/screens/audit_execution_ordering_test.dart` retorna pelo menos `1` (marca de rastreabilidade no header).
    - `grep -c "Supabase" primeaudit/test/screens/audit_execution_ordering_test.dart` retorna `0` (teste é puro).
    - `grep -c "testWidgets" primeaudit/test/screens/audit_execution_ordering_test.dart` retorna `0` (não é teste de widget).
    - `cd primeaudit && flutter test test/screens/audit_execution_ordering_test.dart` exit 0 — todos os testes passam.
  </acceptance_criteria>
  <done>Arquivo de teste unitário criado, 4 testes (3 cenários obrigatórios + 1 extra de múltiplas seções) passam green. Helper `groupAndSort` documenta o comportamento esperado que o Task 2 aplicará ao screen.</done>
</task>

<task type="auto">
  <name>Task 2 (Wave 1): Aplicar fix do sort em _load() de audit_execution_screen.dart</name>
  <files>primeaudit/lib/screens/audit_execution_screen.dart</files>
  <read_first>
    - primeaudit/lib/screens/audit_execution_screen.dart (linhas 57-114, em particular o bloco de agrupamento 73-83)
    - .planning/phases/06-templates/06-PATTERNS.md (seção "primeaudit/lib/screens/audit_execution_screen.dart" — código verbatim "Fixed grouping block — copy this exactly")
    - .planning/phases/06-templates/06-RESEARCH.md (seção "Code Examples → TMPL-01" — código de referência literal)
    - primeaudit/test/screens/audit_execution_ordering_test.dart (arquivo criado no Task 1 — comportamento esperado já está documentado via helper `groupAndSort`)
  </read_first>
  <behavior>
    - Após esta task, os testes do Task 1 continuam GREEN (o helper e o screen agora implementam o mesmo comportamento).
    - `flutter analyze lib/screens/audit_execution_screen.dart` imprime `No issues found!`.
    - `flutter test` (suíte completa) continua GREEN — nenhum teste existente regride.
    - A lógica de negócio no resto de `_load()` permanece 100% inalterada: seção fictícia "Geral" ainda é inserida no topo com `orderIndex = -1`, respostas existentes ainda são populadas, estado final de `_sections` / `_allItems` / `_loading` idêntico exceto pela ordem dos itens dentro dos buckets.
  </behavior>
  <action>
Passo único — Modificar o bloco de agrupamento em `_load()` de `primeaudit/lib/screens/audit_execution_screen.dart`.

Localizar o bloco ATUAL (linhas 73-83 aproximadamente — usar `grep -n "Associa items às seções" primeaudit/lib/screens/audit_execution_screen.dart` para encontrar a linha exata):

REMOVER EXATAMENTE:
```dart
      // Associa items às seções
      final itemsBySection = <String?, List<TemplateItem>>{};
      for (final item in items) {
        itemsBySection.putIfAbsent(item.sectionId, () => []).add(item);
      }
      for (final s in sections) {
        s.items = itemsBySection[s.id] ?? [];
      }

      // Items sem seção ficam numa seção fictícia "Geral"
      final unsectioned = itemsBySection[null] ?? [];
```

INSERIR EXATAMENTE NO MESMO LOCAL:
```dart
      // Associa items às seções — preserva sort por order_index dentro de cada bucket.
      // PostgREST já devolve a lista plana ordenada por order_index, mas o
      // `putIfAbsent + add` distribui os itens em buckets preservando a ordem
      // de iteração (não a ordem relativa por seção). O sort explícito abaixo
      // garante que cada bucket esteja ordenado por orderIndex — fix TMPL-01.
      final itemsBySection = <String?, List<TemplateItem>>{};
      for (final item in items) {
        itemsBySection.putIfAbsent(item.sectionId, () => []).add(item);
      }
      for (final s in sections) {
        final bucket = itemsBySection[s.id] ?? [];
        bucket.sort((a, b) => a.orderIndex.compareTo(b.orderIndex));
        s.items = bucket;
      }

      // Items sem seção ficam numa seção fictícia "Geral"
      final unsectioned = itemsBySection[null] ?? [];
      unsectioned.sort((a, b) => a.orderIndex.compareTo(b.orderIndex));
```

Regras:
- NÃO tocar em nenhuma outra linha de `_load()` — respostas `for (final a in answers)`, guard `mounted`, `setState` com criação da seção fictícia "Geral" ficam idênticos.
- NÃO remover nem alterar outros métodos (`_save`, `_saveAnswer`, `_finalize`, `build`, getters de progresso, etc.).
- NÃO alterar imports — `dart:math`, `material.dart`, models já estão importados; `.sort` é método da `List` (built-in).
- NÃO mudar o `orderIndex` do `TemplateSection` fictício (continua `-1`).
- Manter indentação de 6 espaços (o código dentro de `try` em `_load()` já usa 6 espaços — NÃO subir para 4 nem descer para 8).
- O comentário `// Associa items às seções` vira `// Associa items às seções — preserva sort por order_index dentro de cada bucket.` (substitui a linha de comentário existente por uma mais descritiva; as 4 linhas de explicação subsequentes são NOVAS).
- `bucket.sort` / `unsectioned.sort` — sort in-place na List (não retorna nova lista; muta em lugar). Essa é a API correta da Dart List.
  </action>
  <verify>
    <automated>cd primeaudit && flutter analyze lib/screens/audit_execution_screen.dart &amp;&amp; flutter test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "bucket.sort((a, b) => a.orderIndex.compareTo(b.orderIndex));" primeaudit/lib/screens/audit_execution_screen.dart` retorna `1`.
    - `grep -c "unsectioned.sort((a, b) => a.orderIndex.compareTo(b.orderIndex));" primeaudit/lib/screens/audit_execution_screen.dart` retorna `1`.
    - `grep -c "final bucket = itemsBySection\\[s.id\\] ?? \\[\\];" primeaudit/lib/screens/audit_execution_screen.dart` retorna `1`.
    - `grep -c "s.items = bucket;" primeaudit/lib/screens/audit_execution_screen.dart` retorna `1`.
    - `grep -c "s.items = itemsBySection\\[s.id\\] ?? \\[\\];" primeaudit/lib/screens/audit_execution_screen.dart` retorna `0` (linha antiga removida — era a versão bugada).
    - `grep -c "fix TMPL-01" primeaudit/lib/screens/audit_execution_screen.dart` retorna `1` (rastreabilidade no comentário).
    - `grep -c "final unsectioned = itemsBySection\\[null\\] ?? \\[\\];" primeaudit/lib/screens/audit_execution_screen.dart` retorna `1` (linha ANTERIOR ao novo sort permanece intacta).
    - `grep -c "id: '__unsectioned__'," primeaudit/lib/screens/audit_execution_screen.dart` retorna `1` (seção fictícia "Geral" ainda sendo criada — não removida por engano).
    - `cd primeaudit && flutter analyze lib/screens/audit_execution_screen.dart` imprime `No issues found!`.
    - `cd primeaudit && flutter test` exit 0 (suíte completa green — audit_execution_ordering_test.dart passa + todos os testes existentes passam).
  </acceptance_criteria>
  <done>Fix TMPL-01 aplicado. Buckets de seção e bucket de unsectioned estão ordenados por `orderIndex`. Teste unitário do Task 1 documenta e protege o comportamento. Nenhum outro método de `AuditExecutionScreen` tocado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Este plano NÃO introduz novos trust boundaries. O fix é 100% UI-interno (sort in-memory de `List<TemplateItem>` já carregada). O caminho `getItems(templateId) → PostgREST → RLS` pré-existe e não muda.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | T (Tampering) | Ordem de exibição dos itens na execução | mitigate | Antes do fix, a ordem vista pelo auditor era insertion-order do Map, o que podia divergir silenciosamente de `order_index`. Após o fix, a ordem é determinística e reflete `order_index`. Sem novo surface de tampering. |
| T-06-01-02 | I (Information Disclosure) | Novos logs / telemetria | accept | Nenhum — apenas sort local. Sem `debugPrint` adicionado. |
| T-06-01-03 | D (DoS) | Sort O(n log n) em memória | accept | `n` é o número de itens de uma seção (tipicamente 5-50). Sort é trivial e roda UMA VEZ por `_load()`. Sem loop ou recomputação repetida. |
| T-06-01-04 | — | Access control em `template_items` | accept | RLS existente não é afetada — este plano não faz nenhum SELECT/UPDATE/INSERT novo no backend. RESEARCH.md Security Domain confirma: "No new security surface introduced by this phase." |
</threat_model>

<verification>
- `cd primeaudit && flutter analyze lib/screens/audit_execution_screen.dart` → `No issues found!`.
- `cd primeaudit && flutter test test/screens/audit_execution_ordering_test.dart` → exit 0 (4 testes passam).
- `cd primeaudit && flutter test` → exit 0 (suíte completa green).
- `git diff primeaudit/lib/screens/audit_execution_screen.dart` mostra APENAS alterações no bloco de agrupamento (aproximadamente 8-10 linhas modificadas + 4 linhas novas de comentário). Nenhuma alteração fora do `try` dentro de `_load()`.
- `git status` mostra `primeaudit/test/screens/audit_execution_ordering_test.dart` como untracked (novo).
</verification>

<success_criteria>
- TMPL-01: Na tela de execução de auditoria, as perguntas aparecem na ordem definida por `order_index`, sem embaralhamento. VERIFICÁVEL: grep do `bucket.sort` e `unsectioned.sort` no screen + teste unitário green comprova o comportamento do sort.
- Wave 0 gap fechado: `primeaudit/test/screens/audit_execution_ordering_test.dart` existe e executa.
- Nenhuma regressão: suíte `flutter test` continua verde, código fora de `_load()` intocado.
- Escopo preservado: sem novas migrations, sem novos packages, sem mudanças em service ou model.
</success_criteria>

<output>
After completion, create `.planning/phases/06-templates/06-01-SUMMARY.md` describing:
- Diff resumido de `primeaudit/lib/screens/audit_execution_screen.dart` (linhas adicionadas/alteradas)
- Trecho final do bloco de agrupamento corrigido em `_load()` (para o Plan 06-02 confirmar que o comportamento TMPL-01 está fixado antes de habilitar o drag-and-drop)
- Resultado de `flutter analyze` e `flutter test`
- Status do Wave 0 gap de TMPL-01 (closed)
- Qualquer desvio observado (por exemplo, se o grep de linhas antigas encontrou ocorrência inesperada)
</output>
