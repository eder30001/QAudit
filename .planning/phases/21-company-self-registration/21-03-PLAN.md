---
phase: 21-company-self-registration
plan: 03
type: execute
wave: 2
depends_on:
  - 21-01
  - 21-02
files_modified:
  - primeaudit/lib/screens/register_screen.dart
autonomous: true
requirements:
  - ONBOARD-01

must_haves:
  truths:
    - "Quando _cnpjNotFound == true, aparece botão 'Criar minha empresa' abaixo do badge de erro"
    - "Ao tocar o botão, aparece campo TextFormField com label 'Nome da empresa' e ícone Icons.business_rounded"
    - "O campo nome da empresa é obrigatório quando _wantsToCreateCompany == true"
    - "O CNPJ é obrigatório (não pode estar vazio) quando _wantsToCreateCompany == true"
    - "Ao confirmar, createForRegistration() é chamado antes de signUp(), e o companyId retornado é passado para signUp com role='adm'"
    - "O fluxo de empresa encontrada pelo CNPJ continua funcionando sem alteração"
    - "_companyNameController é descartado no dispose()"
  artifacts:
    - path: "primeaudit/lib/screens/register_screen.dart"
      provides: "RegisterScreen com branch 'Criar minha empresa'"
      contains: "_wantsToCreateCompany"
  key_links:
    - from: "RegisterScreen._register()"
      to: "CompanyService.createForRegistration"
      via: "_companyService.createForRegistration"
      pattern: "_companyService\\.createForRegistration"
    - from: "RegisterScreen._register()"
      to: "AuthService.signUp"
      via: "role: AppRole.adm"
      pattern: "role:.*AppRole\\.adm"
---

<objective>
Atualizar `RegisterScreen` para transformar o estado morto `_cnpjNotFound == true` em fluxo acionável de self-registration:

- Adicionar estado `_wantsToCreateCompany` (bool) e controller `_companyNameController`
- Substituir o container vermelho de "Nenhuma empresa encontrada" por versão expandida com botão "Criar minha empresa"
- Quando expandido: mostrar `TextFormField` para nome da empresa com validação obrigatória
- Atualizar `_register()` para chamar `_companyService.createForRegistration()` → `_authService.signUp(role: AppRole.adm)` no caminho de self-reg
- Tratar erros de `PostgrestException` (ex: CNPJ duplicado) com mensagem em português
- Garantir dispose do novo controller

Purpose: Fechar o gap UX — hoje o usuário que digita CNPJ desconhecido fica preso. Após esta mudança, ele pode criar a empresa e se tornar adm em um fluxo contínuo.
Output: `register_screen.dart` atualizado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/21-company-self-registration/21-RESEARCH.md
@.planning/phases/21-company-self-registration/21-02-SUMMARY.md

<interfaces>
<!-- RegisterScreen estado atual relevante -->
<!-- De: primeaudit/lib/screens/register_screen.dart -->
```dart
// Estado existente (manter tudo)
bool _isLoading = false;
bool _searchingCompany = false;
Company? _foundCompany;
bool _cnpjNotFound = false;

// ADICIONAR após _cnpjNotFound:
bool _wantsToCreateCompany = false;
final _companyNameController = TextEditingController();

// dispose() atual — ADICIONAR _companyNameController.dispose() antes de super.dispose()
@override
void dispose() {
  _nameController.dispose();
  _emailController.dispose();
  _passwordController.dispose();
  _confirmPasswordController.dispose();
  _cnpjController.dispose();
  // ADICIONAR: _companyNameController.dispose();
  super.dispose();
}
```

<!-- CompanyService.createForRegistration (Plan 02) -->
```dart
Future<String> createForRegistration({
  required String cnpj,
  required String name,
}) async { ... } // retorna UUID como String
```

<!-- AuthService.signUp atualizado (Plan 02) -->
```dart
Future<AuthResponse> signUp({
  required String name,
  required String email,
  required String password,
  String? companyId,
  String? role,   // NOVO
}) async { ... }
```

<!-- AppRole.adm -->
```dart
static const String adm = 'adm';
```

<!-- Container existente de _cnpjNotFound (linhas 376-398 do arquivo original) -->
```dart
} else if (_cnpjNotFound) ...[
  const SizedBox(height: 8),
  Container(
    padding: const EdgeInsets.symmetric(horizontal: 14, vertical: 10),
    decoration: BoxDecoration(
      color: AppColors.error.withValues(alpha: 0.06),
      borderRadius: BorderRadius.circular(10),
      border: Border.all(color: AppColors.error.withValues(alpha: 0.3)),
    ),
    child: const Row(
      children: [
        Icon(Icons.info_outline_rounded, color: AppColors.error, size: 18),
        SizedBox(width: 8),
        Expanded(
          child: Text(
            'Nenhuma empresa encontrada com este CNPJ',
            style: TextStyle(fontSize: 12, color: AppColors.error),
          ),
        ),
      ],
    ),
  ),
],
```

<!-- validateCnpj — retorna null para empty (campo opcional por default) -->
```dart
String? validateCnpj(String? value) {
  if (value == null || value.trim().isEmpty) return null; // opcional por padrão
  // ...validação completa...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar estado, dispose e lógica _register() para self-registration</name>
  <files>primeaudit/lib/screens/register_screen.dart</files>
  <read_first>
    - primeaudit/lib/screens/register_screen.dart (ler inteiro — entender dispose(), _register(), imports existentes)
    - primeaudit/lib/core/app_roles.dart (verificar constante AppRole.adm)
  </read_first>
  <action>
Fazer as seguintes modificações em `primeaudit/lib/screens/register_screen.dart`:

**1. Verificar imports — adicionar se ausente:**
O arquivo já importa `app_roles.dart`? Se não, adicionar:
```dart
import '../core/app_roles.dart';
```
(Verificar: o import de `company_service.dart` e `auth_service.dart` já existem — não duplicar.)

**2. Adicionar campos de estado após `bool _cnpjNotFound = false;`:**
```dart
  bool _wantsToCreateCompany = false;
  final _companyNameController = TextEditingController();
```

**3. Atualizar `dispose()` — adicionar `_companyNameController.dispose()` ANTES de `super.dispose()`:**
```dart
  @override
  void dispose() {
    _nameController.dispose();
    _emailController.dispose();
    _passwordController.dispose();
    _confirmPasswordController.dispose();
    _cnpjController.dispose();
    _companyNameController.dispose();
    super.dispose();
  }
```

**4. Substituir o método `_register()` completo pelo seguinte:**
```dart
  Future<void> _register() async {
    if (!_formKey.currentState!.validate()) return;

    setState(() => _isLoading = true);

    try {
      String? companyId = _foundCompany?.id;

      // Caminho de self-registration: criar empresa antes de criar usuário
      if (_wantsToCreateCompany && _cnpjNotFound) {
        companyId = await _companyService.createForRegistration(
          cnpj: _cnpjController.text.trim(),
          name: _companyNameController.text.trim(),
        );
      }

      final response = await _authService.signUp(
        name: _nameController.text.trim(),
        email: _emailController.text.trim(),
        password: _passwordController.text,
        companyId: companyId,
        role: (_wantsToCreateCompany && companyId != null) ? AppRole.adm : null,
      );

      if (!mounted) return;

      if (response.user != null) {
        if (response.session == null) {
          _showInfo(
            'Cadastro realizado! Verifique seu e-mail para confirmar a conta.',
          );
          Navigator.of(context).pop();
        } else {
          Navigator.of(context).pushReplacement(
            MaterialPageRoute(builder: (_) => const HomeScreen()),
          );
        }
      }
    } on PostgrestException catch (e) {
      if (!mounted) return;
      _showError(_translateRegistrationError(e.message));
    } on AuthException catch (e) {
      if (!mounted) return;
      _showError(_translateError(e.message));
    } catch (e) {
      if (!mounted) return;
      _showError('Erro inesperado. Tente novamente.');
    } finally {
      if (mounted) setState(() => _isLoading = false);
    }
  }
```

**5. Adicionar método `_translateRegistrationError` após `_translateError`:**
```dart
  String _translateRegistrationError(String message) {
    if (message.contains('CNPJ já cadastrado')) {
      return 'Este CNPJ já está cadastrado. Se você já criou a empresa, tente fazer login ou use a opção de recuperação de senha.';
    }
    if (message.contains('CNPJ deve ter 14')) return 'CNPJ inválido. Verifique o número digitado.';
    if (message.contains('Nome da empresa é obrigatório')) return 'Informe o nome da empresa.';
    return 'Erro ao criar empresa: $message';
  }
```

**6. Adicionar `supabase_flutter` import se `PostgrestException` não estiver disponível:**
O arquivo já importa `supabase_flutter` (linha 2 do arquivo original: `import 'package:supabase_flutter/supabase_flutter.dart';`) — `PostgrestException` vem deste pacote.

**7. Ao resetar `_cnpjNotFound` (em `_searchCompany`), também resetar `_wantsToCreateCompany`:**
No método `_searchCompany`, na chamada `setState` que limpa `_foundCompany` e `_cnpjNotFound`, adicionar `_wantsToCreateCompany = false`:
```dart
    setState(() {
      _foundCompany = null;
      _cnpjNotFound = false;
      _wantsToCreateCompany = false; // ADICIONAR
    });
```
(Há dois setState em _searchCompany — aplicar no primeiro, onde o CNPJ ainda tem < 14 dígitos, e também no segundo, após o try/catch.)
  </action>
  <verify>
    <automated>cd primeaudit && flutter analyze lib/screens/register_screen.dart</automated>
  </verify>
  <done>
    - `register_screen.dart` contém `bool _wantsToCreateCompany = false`
    - `register_screen.dart` contém `final _companyNameController = TextEditingController()`
    - `dispose()` chama `_companyNameController.dispose()`
    - `_register()` chama `_companyService.createForRegistration` quando `_wantsToCreateCompany && _cnpjNotFound`
    - `_authService.signUp` recebe `role: AppRole.adm` no caminho de self-reg
    - `_translateRegistrationError` existe e trata 'CNPJ já cadastrado'
    - `flutter analyze` retorna sem erros
  </done>
</task>

<task type="auto">
  <name>Task 2: Atualizar UI de _cnpjNotFound com branch "Criar minha empresa"</name>
  <files>primeaudit/lib/screens/register_screen.dart</files>
  <read_first>
    - primeaudit/lib/screens/register_screen.dart (necessário para Task 2 — ler novamente pois foi modificado na Task 1)
  </read_first>
  <action>
Substituir o bloco `} else if (_cnpjNotFound) ...[` no método `_buildForm()` pelo seguinte (manter o bloco `if (_foundCompany != null)` intacto acima):

```dart
          ] else if (_cnpjNotFound) ...[
            const SizedBox(height: 8),
            // Badge de erro — empresa não encontrada
            Container(
              padding: const EdgeInsets.symmetric(horizontal: 14, vertical: 10),
              decoration: BoxDecoration(
                color: AppColors.error.withValues(alpha: 0.06),
                borderRadius: BorderRadius.circular(10),
                border: Border.all(color: AppColors.error.withValues(alpha: 0.3)),
              ),
              child: Row(
                children: [
                  const Icon(Icons.info_outline_rounded, color: AppColors.error, size: 18),
                  const SizedBox(width: 8),
                  const Expanded(
                    child: Text(
                      'Nenhuma empresa encontrada com este CNPJ',
                      style: TextStyle(fontSize: 12, color: AppColors.error),
                    ),
                  ),
                  if (!_wantsToCreateCompany)
                    TextButton(
                      onPressed: () => setState(() => _wantsToCreateCompany = true),
                      style: TextButton.styleFrom(
                        padding: const EdgeInsets.symmetric(horizontal: 8, vertical: 4),
                        minimumSize: Size.zero,
                        tapTargetSize: MaterialTapTargetSize.shrinkWrap,
                      ),
                      child: const Text(
                        'Criar empresa',
                        style: TextStyle(
                          fontSize: 12,
                          fontWeight: FontWeight.w600,
                          color: AppColors.primary,
                        ),
                      ),
                    ),
                ],
              ),
            ),
            // Seção de criação de empresa (visível quando _wantsToCreateCompany == true)
            if (_wantsToCreateCompany) ...[
              const SizedBox(height: 12),
              TextFormField(
                controller: _companyNameController,
                textCapitalization: TextCapitalization.words,
                textInputAction: TextInputAction.done,
                onFieldSubmitted: (_) => _register(),
                decoration: _inputDecoration(
                  label: 'Nome da empresa',
                  hint: 'Ex: Acme Indústria Ltda',
                  icon: Icons.business_rounded,
                ).copyWith(
                  suffixIcon: IconButton(
                    icon: Icon(
                      Icons.close,
                      color: AppTheme.of(context).textSecondary,
                      size: 20,
                    ),
                    onPressed: () => setState(() {
                      _wantsToCreateCompany = false;
                      _companyNameController.clear();
                    }),
                    tooltip: 'Cancelar criação de empresa',
                  ),
                ),
                validator: (v) {
                  if (!_wantsToCreateCompany) return null;
                  if (v == null || v.trim().isEmpty) return 'Informe o nome da empresa';
                  return null;
                },
              ),
              const SizedBox(height: 6),
              Row(
                children: [
                  const Icon(Icons.info_outline, size: 13, color: AppColors.primary),
                  const SizedBox(width: 4),
                  Expanded(
                    child: Text(
                      'Você será cadastrado como administrador com 30 dias de avaliação gratuita.',
                      style: TextStyle(fontSize: 11, color: AppTheme.of(context).textSecondary),
                    ),
                  ),
                ],
              ),
            ],
          ],
```

**Atualizar o validator do campo CNPJ** para exigir CNPJ quando criando empresa.

O campo CNPJ atualmente usa `validator: validateCnpj` diretamente (linha da declaration em `_buildForm`). Substituir por:
```dart
            validator: (v) {
              if (_wantsToCreateCompany) {
                if (v == null || v.trim().isEmpty) return 'CNPJ é obrigatório para criar empresa';
                return validateCnpj(v);
              }
              return validateCnpj(v);
            },
```

**Verificação final de consistência:**
- O bloco `if (_foundCompany != null) ...[` permanece intacto acima do bloco `else if (_cnpjNotFound)`
- Os dois blocos estão dentro do mesmo `Column` em `_buildForm()`
- `_wantsToCreateCompany` é resetado ao digitar novo CNPJ (Task 1 já faz isso em `_searchCompany`)
  </action>
  <verify>
    <automated>cd primeaudit && flutter analyze lib/screens/register_screen.dart</automated>
  </verify>
  <done>
    - O arquivo contém `_wantsToCreateCompany` em dois contextos: toggle de visibilidade + validator
    - O campo `_companyNameController` é usado no TextFormField de 'Nome da empresa'
    - O botão "Criar empresa" aparece dentro do container de erro de CNPJ
    - O botão X fecha a seção (setState _wantsToCreateCompany = false + clear controller)
    - O validator do CNPJ exige valor quando `_wantsToCreateCompany == true`
    - O validator do Nome da empresa exige valor quando `_wantsToCreateCompany == true`
    - `flutter analyze lib/screens/register_screen.dart` retorna sem erros
    - `flutter test` (suite completa) verde — nenhum teste existente quebrado
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Usuário → RegisterScreen | Entrada de CNPJ e nome da empresa sem autenticação — validação client-side + server-side |
| RegisterScreen → createForRegistration | Chamada pré-auth com dados do usuário |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-07 | Tampering | CNPJ validator / self-reg path | mitigate | Validator wrapper exige CNPJ não-vazio + válido quando _wantsToCreateCompany == true; DB faz segunda validação |
| T-21-08 | Elevation of Privilege | signUp role param / AppRole.adm | mitigate | Role 'adm' só é passado quando _wantsToCreateCompany && companyId != null; DB trigger clampeia mesmo assim (defesa em profundidade) |
| T-21-09 | Denial of Service | Botão "Criar empresa" / companyNameController | accept | Usuário pode chamar createForRegistration + signUp falhar: empresa órfã. Comentário no código documenta edge case. Rate limit do Supabase mitiga abuso. |
</threat_model>

<verification>
```
cd primeaudit
flutter analyze lib/screens/register_screen.dart
flutter test
```

Verificação funcional manual (após aplicar migration no Supabase):
1. Abrir tela de cadastro
2. Digitar CNPJ válido mas inexistente (ex: 11.222.333/0001-81) → badge de erro aparece COM botão "Criar empresa"
3. Tocar "Criar empresa" → campo "Nome da empresa" aparece com botão X
4. Tocar X → campo some, botão "Criar empresa" reaparece
5. Preencher nome + dados pessoais → tocar "Cadastrar" → empresa criada + usuário criado como adm
6. Digitar CNPJ de empresa EXISTENTE → badge verde aparece, SEM botão "Criar empresa" (fluxo original intacto)
</verification>

<success_criteria>
- `register_screen.dart` contém `_wantsToCreateCompany`, `_companyNameController`, `_translateRegistrationError`
- `_companyNameController.dispose()` está em `dispose()`
- `_register()` chama `createForRegistration` antes de `signUp` quando em modo self-reg
- `signUp` recebe `role: AppRole.adm` somente no caminho de self-reg
- CNPJ required quando `_wantsToCreateCompany == true`
- Nome da empresa required quando `_wantsToCreateCompany == true`
- Fluxo CNPJ encontrado não é afetado (nenhum branch de `_foundCompany != null` foi tocado)
- `flutter analyze` sem erros; `flutter test` verde
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/21-company-self-registration/21-03-SUMMARY.md`
</output>
